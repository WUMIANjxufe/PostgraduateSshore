
<view wx:for="{{play}}" wx:key="id">
<!-- <view wx:for="{{play}}" wx:key="id" wx:for-item="me"></view> -->
  <!-- <view>{{me.name}}</view> -->
  <view>{{item.name}}</view>
  <view>{{item.phonenumber}}</view>
  <view>{{item.address}}</view>
</view>
<view>  <button bindtap='bindtest'>test</button> </view>